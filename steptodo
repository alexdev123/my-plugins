<?php
/*
__PocketMine Plugin__
 name=no more /me is now op
 description=
 version=0.0.1
 author=alex
 class=op
 apiversion=10,11,12,13
 */
class op implements Plugin {
  private $api, $path, $d;
  public function __construct(ServerAPI $api, $server = false) {
    $this->api = $api;
  }

public function init(){

$this->api->console->register("me is now op","kick the player who uses it",array($this,"command"));
}
public function command($cmd, $params, $issuer, $args, $issuer){
console->
